"use strict";

const id = document.querySelector("#id"), // #을 붙이면 id값에 담긴 선택자를 가져온다는 뜻.
  psword = document.querySelector("#pwword"),
loginBtn = document.querySelector("button"); //button 태그를 통째로 불러오는거임.
  

loginBtn.addEventListener("click", login);

function login(){
console.log("bye");
};



